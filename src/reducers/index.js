//import actions here

const defaultState = {
  //initial states
  description: ""
};

let reducer = (state = defaultState, action) => {
  switch (action.type) {
    // case statement

    default:
      return state;
  }
};

export default reducer;
